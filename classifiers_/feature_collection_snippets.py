            #############################################
            # feat_weight = clf.coef_.mean(axis=0)
            # self.all_feature_weights.append(feat_weight)

            #############################################
            # feat_weight = clf.coef_.mean(axis=0)
            # self.all_feature_weights.append(feat_weight)
            #############################################

# Add in classifier object to store so dont need to pass through other variables